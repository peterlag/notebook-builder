import { createFeatureSelector, createSelector } from "@ngrx/store";
// import { welcomeStateFeatureKey } from "../reducers/step-by-step.reducer";

// const welcomeViewState = createFeatureSelector<boolean>(welcomeStateFeatureKey);

// export const stateWelcome = createSelector(
//     welcomeViewState,
//     (welcomeViewState) => welcomeViewState
// );